# spread firefox
--user-agent "Mozilla/5.0 (X11; U; Linux i686; en-US; rv:1.9.2.13) Gecko/20101203 Firefox/3.6.13 ; wounded/0.0"

# "i download cookies" -- ziggy
# "cookies are for sometimes" -- cookie monster
--cookie ~/.curlrc.cookies
--cookie-jar ~/.curlrc.cookies

# change the referrer when following 302's
#--referer ;auto

# Accept-Encoding: deflate, gzip
--compressed
