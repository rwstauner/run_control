#!/bin/bash

. `dirname "$0"`/../scripts/.helpers.sh

script $PREFIX/bin/node <<'STUB'
#!/bin/sh
echo "${0##*/} not installed" >&2
exit 1
STUB

for i in \
  npm \
; do
  ln -sf node $PREFIX/bin/$i
done
